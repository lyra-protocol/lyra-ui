import { LandingHero } from "@/components/landing-hero";

/**
 * lyrabuild.xyz
 *
 * Being rebuilt one section at a time. The hero is done; the rest follows
 * deliberately rather than all at once, so each part earns its place before
 * the next is drawn.
 */
export default function HomePage() {
  return (
    <main>
      <LandingHero />
    </main>
  );
}
